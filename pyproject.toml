[project]
name = "bart-gpu-article"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bartz",
    "ipython>=9.9.0",
    "matplotlib>=3.10.8",
    "matplotlib-label-lines>=0.8.1",
    "polars[pandas]>=1.37.1",
    "rpy2>=3.6.4",
    "scikit-learn>=1.8.0",
    "xgboost>=3.1.3",
]

[tool.uv.sources]
bartz = { git = "https://github.com/bartz-org/bartz.git", rev = "main" }

[tool.ruff.lint]
ignore = ["F722"]
